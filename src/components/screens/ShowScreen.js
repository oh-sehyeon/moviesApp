import MediaInfoContainer from '../containers/MediaInfoContainer'

const ShowScreen = ({ navigation, route }) => (
  <MediaInfoContainer navigation={navigation} route={route} />
);

export default ShowScreen
